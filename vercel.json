{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd nextjs_space && yarn install --frozen-lockfile && yarn build",
  "installCommand": "cd nextjs_space && yarn install --frozen-lockfile",
  "outputDirectory": "nextjs_space/.next",
  "framework": "nextjs",
  "devCommand": "cd nextjs_space && yarn dev"
}
