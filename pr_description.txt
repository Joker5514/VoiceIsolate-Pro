🧹 [code health] Remove dev server startup ASCII art logs

🎯 **What:** Removed the 6 lines of dev server startup ASCII art (`console.log`) from the `app.listen()` block in `server.js`.
💡 **Why:** Standardizing logging is good practice. Dev-centric `console.log` statements meant for local development debugging clutter up the output in production and violate logging best practices.
✅ **Verification:** Verified that the syntax changes are valid via `node --check server.js`, the structural validation script passes (`node scripts/validate.js`), and existing unrelated tests run via `pnpm test`.
✨ **Result:** Improved codebase maintainability by cleaning up unnecessary production terminal output.
