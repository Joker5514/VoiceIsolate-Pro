🧹 [code health improvement] Remove duplicated utility methods to fix lint error

🎯 **What:**
Removed duplicate declarations of `calcRMS`, `calcPeak`, and `fmtDur` at the end of the `VoiceIsolatePro` class in `public/app/app.js`.

💡 **Why:**
A previous, hallucinated automated refactoring mistakenly placed duplicate copies of these utility methods at the bottom of the file (lines 2856-2858), leaving the original methods correctly positioned higher up (lines 2687-2689). This duplication caused `eslint` to trigger a `no-dupe-class-members` fatal error, blocking the GitHub Actions CI pipeline. Removing the duplicates resolves the linting failure without altering runtime behavior.

✅ **Verification:**
Verified that `pnpm run lint` now completes without errors. Tests pass via `pnpm test` and the validation script via `pnpm run validate` completes successfully.

✨ **Result:**
The CI pipeline failure is resolved, restoring code health and unlocking subsequent deployment steps.
