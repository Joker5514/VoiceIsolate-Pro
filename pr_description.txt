🧹 [code health improvement] Fix Vercel validation failure

🎯 **What:**
Added the missing `"outputDirectory": "public"` configuration to the root `vercel.json` file.

💡 **Why:**
The structural validation script (`scripts/validate.js`) explicitly checks for this configuration property to ensure deployments correctly serve static assets from the `public` directory rather than the repository root. This prevents the build from failing during CI and ensures correct environment configuration for Vercel.

✅ **Verification:**
Verified that `pnpm run validate` now passes completely, returning a "✅ All checks passed" status, and all unit tests continue to pass via `pnpm test`.

✨ **Result:**
The repository validation checks are now fully passing and the Vercel deployment structure is explicitly defined.
