🧹 [code health improvement] Fix empty catch block in updateLiveChain

🎯 **What:**
Replaced an empty `catch(e) {}` block inside the `updateLiveChain` method in `app.js` with a proper `console.error` log.

💡 **Why:**
An empty catch block silently swallows any exceptions that occur during Web Audio API automation (e.g., `setTargetAtTime`), hiding potential failures such as invalid target values, missing audio nodes, or timing errors. Logging the error improves the maintainability of the codebase by properly surfacing these issues, making audio pipeline debugging significantly easier without crashing the application.

✅ **Verification:**
Verified that the file syntax remains correct (`node --check app.js`) and all audio DSP pipeline tests pass successfully via `pnpm test`. The changes safely propagate previously silenced errors to the console without altering the intended DSP behavior.

✨ **Result:**
The application's real-time Web Audio automation loop now gracefully logs any encountered parameter update errors, resolving a hidden failure vector and improving overall code health.
