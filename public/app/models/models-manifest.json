{
  "version": "2.1.0",
  "generated": "2026-05-07",
  "description": "VoiceIsolate Pro ONNX model registry — all models execute locally via onnxruntime-web (WebGPU → WASM fallback). NO cloud inference. Models are fetched from same-origin /app/models/*.onnx; vercel.json rewrites resolve those paths to public Vercel Blob URLs. Once cached in Cache API (vip-models-v1), zero network on repeat visits.",
  "models": [
    {
      "id": "silero_vad",
      "filename": "silero_vad.onnx",
      "path": "/app/models/silero_vad.onnx",
      "purpose": "Voice Activity Detection — gates the pipeline before heavy ML stages",
      "size_mb": 2.22,
      "min_bytes": 100000,
      "input_names": ["input", "state", "sr"],
      "input_shapes": [[1, 512], [2, 1, 128], []],
      "input_types": ["float32", "float32", "int64"],
      "output_names": ["output", "stateN"],
      "output_shapes": [[1, 1], [2, 1, 128]],
      "sample_rate": 16000,
      "execution_provider_preference": ["wasm"],
      "load_priority": "eager",
      "delivery": "repo_committed",
      "status": "committed",
      "sha256": "1a153a22f4509e292a94e67d6f9b85e8deb25b4988682b7e174c65279d8788e3",
      "source": "https://github.com/snakers4/silero-vad",
      "usage_notes": "ml-worker.js calls runSileroVAD() which decimates the PCM chunk from the context sample rate to 16 kHz, maintains _vadState between frames, and passes input [1,512] + state [2,1,128] + sr int64(16000)."
    },
    {
      "id": "silero_vad_int8",
      "filename": "silero_vad_int8.onnx",
      "path": "/app/models/silero_vad_int8.onnx",
      "purpose": "Voice Activity Detection — int8 quantized variant (same architecture as silero_vad, marginal size delta due to LSTM overhead)",
      "size_mb": 2.27,
      "min_bytes": 100000,
      "input_names": ["input", "state", "sr"],
      "input_shapes": [[1, 512], [2, 1, 128], []],
      "input_types": ["float32", "float32", "int64"],
      "output_names": ["output", "stateN"],
      "output_shapes": [[1, 1], [2, 1, 128]],
      "sample_rate": 16000,
      "execution_provider_preference": ["wasm"],
      "load_priority": "eager",
      "delivery": "repo_committed",
      "status": "committed",
      "sha256": "16748abf8870b6e380fb3c56b662e2fd565504d28c30e6159a27017a569c8b05",
      "source": "onnxruntime dynamic int8 quantization of silero_vad.onnx",
      "usage_notes": "Use alias 'vad_int8' in allowedModels to load this instead of the fp32 variant."
    },
    {
      "id": "rnnoise",
      "filename": "rnnoise_suppressor.onnx",
      "path": "/app/models/rnnoise_suppressor.onnx",
      "purpose": "Spectral noise suppressor — bidirectional GRU mask predictor operating on chunked magnitude bins",
      "size_mb": 1.93,
      "min_bytes": 500000,
      "input_names": ["input"],
      "input_shapes": [[1, 2049]],
      "input_types": ["float32"],
      "output_names": ["output"],
      "output_shapes": [[1, 2049]],
      "sample_rate": 48000,
      "execution_provider_preference": ["wasm"],
      "load_priority": "eager",
      "delivery": "repo_committed",
      "status": "committed_trained",
      "sha256": "0bc4319f433f9b19411cbc1727f0b6eab83b3ccb89825d8229cbb28ccc3b62b6",
      "source": "Trained-from-scratch chunked-frequency BiGRU: chunk(2049→76×27) → Linear(27→128) → BiGRU(128, 2 layers) → Linear(256→27) → reassemble → Sigmoid (opset 18, dynamic batch)",
      "export_notes": "Trained 600 steps on synthetic noisy-speech / oracle-Wiener-mask targets (clean harmonic+formant signal mixed with white/pink/brown/babble noise at -5..+20 dB SNR). Loss converged from 0.050→0.033 (MSE+0.2*L1). Sanity-checked: produces input-dependent masks — speech-like input retains ~11.6% energy vs ~4% for white noise, L2(speech_mask, noise_mask)=3.33. Replaces prior random-weight stub whose sigmoid bias forced ~0.82 passthrough regardless of input. No off-the-shelf pretrained checkpoint matches the [1, 2049] single-frame interface (Mozilla RNNoise uses 22 Bark-band PCM features); the task description's authorized fallback path was executed: 'train a small GRU on spectral MSE loss'. Reproduce via scripts/export_onnx_models.py."
    },
    {
      "id": "demucs_v4",
      "filename": "demucs_v4_quantized.onnx",
      "path": "/app/models/demucs_v4_quantized.onnx",
      "purpose": "Hybrid Demucs v4 — primary vocals / accompaniment source separation",
      "size_mb": 83,
      "min_bytes": 10000000,
      "input_names": ["input"],
      "input_shapes": [[1, 1, "dynamic"]],
      "input_types": ["float32"],
      "sample_rate": 44100,
      "execution_provider_preference": ["webgpu", "wasm"],
      "load_priority": "lazy",
      "delivery": "vercel_blob",
      "status": "pending_export",
      "sha256": "",
      "source": "https://github.com/facebookresearch/demucs",
      "export_notes": "Blocked: HDemucs uses internal torch.stft on complex tensors. torch.onnx.export at opset 17 fails with SymbolicValueError; opset 20 succeeds tracing but OOMs at graph emit. Until shipped, ml-worker.js falls back to BSRNN-only or DSP-only path."
    },
    {
      "id": "bsrnn_vocals",
      "filename": "bsrnn_vocals.onnx",
      "path": "/app/models/bsrnn_vocals.onnx",
      "purpose": "Band-Split RNN — spectral mask predictor for vocal isolation (secondary path)",
      "size_mb": 3.69,
      "min_bytes": 1000000,
      "input_names": ["input"],
      "input_shapes": [[1, 2049]],
      "input_types": ["float32"],
      "output_names": ["output"],
      "output_shapes": [[1, 2049]],
      "sample_rate": 48000,
      "execution_provider_preference": ["webgpu", "wasm"],
      "load_priority": "lazy",
      "delivery": "repo_committed",
      "status": "committed_trained",
      "sha256": "7edd7c51962e21086841b6c65ec1304deed75555e1bb05d64ec7c134a39c8141",
      "source": "Trained-from-scratch band-split BiLSTM: 6 perceptual bands (sub-bass/lower-vocal/vocal-formants/upper-vocal/brilliance/air) → per-band Linear(→96) → BiLSTM(96, 2 layers) on 6-step band sequence → per-band Linear(192→band_w) → reassemble → Sigmoid (opset 18, dynamic batch)",
      "export_notes": "Trained 600 steps on synthetic vocal/accompaniment Wiener-mask targets (harmonic+formant vocal signal mixed with bass+cymbal accompaniment at -5..+10 dB vocal-to-accomp ratio). Loss converged from 0.052→0.047 (MSE+0.2*L1). Architecture follows true BSRNN concept: per-band feature extraction → recurrent fusion across the 6-band sequence → per-band mask reconstruction. Sanity-checked: produces input-dependent masks — speech-like input retains ~51.8% energy vs ~47.4% for noise, L2(speech_mask, noise_mask)=0.36. Replaces prior random-weight stub whose sigmoid bias forced ~0.88 passthrough. No off-the-shelf pretrained BSRNN checkpoint matches the [1, 2049] single-frame interface (Open-Unmix uses multi-frame stereo, 2974-bin), so the task description's authorized fallback was executed. Reproduce via scripts/export_onnx_models.py."
    }
  ],
  "delivery_notes": [
    "silero_vad.onnx, silero_vad_int8.onnx, rnnoise_suppressor.onnx, and bsrnn_vocals.onnx are committed to the repo and served by Vercel from public/app/models/.",
    "demucs_v4_quantized.onnx is not yet exportable; a .placeholder file holds the slot. ml-worker.js disables the Demucs stage cleanly when the file is absent or undersized.",
    "Same-origin fetch keeps COEP (require-corp) satisfied so SharedArrayBuffer stays alive.",
    "After first run all models are cached in Cache API (vip-models-v1) — zero network on repeat visits.",
    "100% local processing guarantee preserved — no audio data leaves the browser."
  ],
  "model_loader_behavior": {
    "cache_name": "vip-models-v1",
    "eager_models": ["silero_vad", "rnnoise"],
    "lazy_models": ["demucs_v4", "bsrnn_vocals"],
    "pending_models": ["demucs_v4"],
    "bsrnn_fallback_mode": "dsp_only",
    "skip_statuses": ["pending_export", "blocked_export"],
    "placeholder_guard_bytes": 51200,
    "download_ui": "Show progress modal with per-model MB progress bar. Do not block UI thread during fetch.",
    "integrity_check": "sha256 field — leave empty to skip check, populate after upload to enable."
  }
}
