# This file ensures public/lib/ is tracked by git.
# The actual ORT assets (ort.min.js, *.wasm) are generated by:
#   pnpm install  (runs scripts/setup-ort.js via postinstall)
# They are listed in .gitignore to avoid committing binary WASM to the repo.
